{% extends 'admin-base.html.twig' %}
{% block body %}
    <div class="container">
        <div class="d-none d-md-block">
            <div class="row">
                <div class="col-md-6">
                    <h1>
                        {{ log.createdAt|date('d.m.Y H:i') }}
                    </h1>
                </div>
            </div>
        </div>
        <div class="row">
            <div class="col">
                {{ log.message }}
            </div>
        </div>
    </div>
{% endblock %}
