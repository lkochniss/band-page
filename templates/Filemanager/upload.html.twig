{% extends 'admin-base.html.twig' %}
{% block body %}
    <div class="container">
        <h1>{% trans from "messages" %}upload_image{% endtrans %}</h1>
        {{ form_start(form) }}
        <div class="form-row">
            <div class="form-group col-md-4 offset-3">
                <label>{% trans from "messages" %}name{% endtrans %}</label>
                {{ form_widget(form.name) }}
            </div>
        </div>
        <div class="form-row">
            <div class="form-group col-md-4 offset-3">
                <label>{% trans from "messages" %}file{% endtrans %}</label>
                {{ form_widget(form.file) }}
            </div>
        </div>
        <div class="form-row">
            <div class="col-md-4 offset-3">
                {{ form_row(form.submit) }}
            </div>
        </div>
        {{ form_end(form) }}
    </div>
{% endblock %}
