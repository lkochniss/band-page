{% extends 'base.html.twig' %}
{% block body %}
    <div class="container">
        <div class="row">
            <h1 class="col">{{ entity.title }}</h1>
        </div>
        <hr/>
        <div class="row">
            <div class="col">
                {{ entity.content| raw }}
            </div>
        </div>
    </div>
{% endblock %}
