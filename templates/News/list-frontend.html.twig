{% extends 'base.html.twig' %}
{% block body %}
    <div class="container">
        {% for post in entities %}

            <div class="card">
                <div class="card-header">
                    <div class="text-right text-muted">{{ post.createdAt|date('d.m.Y') }}</div>
                    <h2 class="card-title">{{ post.title }}</h2>
                </div>
                <div class="card-body">
                    <p class="card-text">
                        {{ post.content }}
                    </p>
                    <a href="{{ path('news_show', {'slug': post.slug}) }}"
                       class="btn btn-primary">{% trans from 'news' %}Read more{% endtrans %}</a>
                </div>
            </div>
        {% endfor %}
    </div>
{% endblock %}
